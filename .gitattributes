# Store the batch file byte for byte, CRLF included. With "text eol=crlf" git
# would keep LF in the repository, and a ZIP download would carry LF into
# cmd.exe, where goto/label handling gets unreliable.
*.bat -text

*.zip binary
*.png binary
*.ico binary

*.py  text eol=lf
*.md  text eol=lf
