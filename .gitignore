# local configuration -- holds API keys and passwords
config.json
gui.json

__pycache__/
*.py[cod]

# which line this working copy follows -- an installation detail, not code
channel.json

# the packages hang on the GitHub release, not in the repository
releases/
